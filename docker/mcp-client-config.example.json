{
  "_comment": "Drop into Claude Desktop's claude_desktop_config.json (or your app's MCP config). Pick ONE of the two servers.",
  "mcpServers": {
    "oxide-triage": {
      "command": "oxide-triage",
      "args": ["mcp"],
      "env": {
        "OXIDE_TRIAGE_CACHE": "/absolute/path/to/data/cache.sqlite",
        "OXIDE_TRIAGE_OFFLINE": "1"
      }
    },
    "oxide-triage-http": {
      "url": "http://127.0.0.1:8765/mcp"
    }
  }
}
