# Runtime settings for an offline (air-gapped) install. Copied to .env beside docker/ in a
# release; nothing here is a secret and no key is needed: the cache ships already warmed.
LLM_PROVIDER=none
OXIDE_TRIAGE_OFFLINE=1
OXIDE_TRIAGE_ADMIN=0
OXIDE_TRIAGE_CACHE=/data/cache.sqlite
