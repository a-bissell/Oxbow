__pycache__/
*.pyc
.DS_Store
*.egg-info/
build/
dist/
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/
/data/
eval/output/
.streamlit/secrets.toml
Archive.zip


# The built front end is committed on purpose: pip, the Docker image and the offline release
# all serve it and none of them runs Node. CI rebuilds it from ui/src and fails when the
# committed bundle is stale (job `ui` in .github/workflows/ci.yml).
!oxide_triage/ui/dist/
!oxide_triage/ui/dist/**
